version: "3.9"

services:
  backend:
    build: ./backend
    ports:
      - "5000:5000"
    volumes:
      - ./backend:/app
######## required on server only

#     networks:
#       - spectacle-shared-network

# networks:
#   spectacle-shared-network:
#     external: true