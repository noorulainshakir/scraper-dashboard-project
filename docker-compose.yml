version: "3.9"

services:
  backend:
    build: ./backend
    ports:
      - "5000:5000"
    volumes:
      - ./backend:/app
    networks:
      - spectacle-shared-network

networks:
  spectacle-shared-network:
    external: true